"""
Reserved for route modules.

The service currently exposes three endpoints (/health, /forecast, /optimize)
and they live in app/main.py, where they are easier to read together than split
across files. When a fourth endpoint arrives, split them into modules here.
"""
